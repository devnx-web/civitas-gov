---
phase: 11-fundacao-infra-cruzada
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/lib/log-acesso.ts
  - src/lib/data/log-acesso.ts
  - src/auth.ts
  - src/lib/actions/auth.ts
  - src/app/(app)/auditoria/log-acesso/page.tsx
autonomous: true
requirements: [AUDIT-01, AUDIT-02]
must_haves:
  truths:
    - "Cada login bem-sucedido grava um registro em log_acesso com evento LOGIN_SUCESSO, IP e user-agent"
    - "Cada login falho grava um registro com evento LOGIN_FALHA"
    - "Cada logout grava um registro com evento LOGOUT"
    - "Refresh de sessão a cada navegação NÃO gera registro (evita explosão de linhas)"
    - "O administrador abre a tela de log de acessos e filtra por usuário, período e tipo de evento"
  artifacts:
    - path: "src/lib/log-acesso.ts"
      provides: "Helper registrarAcesso — grava trilha de acesso de forma tolerante a erro"
      exports: ["registrarAcesso"]
    - path: "src/lib/data/log-acesso.ts"
      provides: "Query filtrável listarLogAcesso com paginação"
      exports: ["listarLogAcesso"]
    - path: "src/app/(app)/auditoria/log-acesso/page.tsx"
      provides: "Tela RSC de consulta do log de acessos com filtros"
  key_links:
    - from: "src/auth.ts"
      to: "src/lib/log-acesso.ts"
      via: "events.signIn / events.signOut chamam registrarAcesso"
      pattern: "registrarAcesso"
    - from: "src/app/(app)/auditoria/log-acesso/page.tsx"
      to: "src/lib/data/log-acesso.ts"
      via: "chamada de listarLogAcesso com filtros de searchParams"
      pattern: "listarLogAcesso"
---

<objective>
Implementar B3 — a tabela `LogAcesso` dedicada com gravação automática de login/logout/falha e a tela administrativa de consulta filtrável.

Purpose: Rastreabilidade de acessos é exigência LGPD (REQ-NF-013/014). LogAcesso é a primeira peça observável da fila de fundação — alto valor, baixo custo, isolada.

Output: helper `registrarAcesso`, listeners no NextAuth, data-layer filtrável, página de consulta para o administrador.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-fundacao-infra-cruzada/11-CONTEXT.md
@.planning/phases/11-fundacao-infra-cruzada/11-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Padrões do codebase — extraídos dos arquivos canônicos. -->

NextAuth v5 (src/auth.ts): `export const { handlers, auth, signIn, signOut } = NextAuth({ ...authConfig, providers: [Credentials({...})] })`. O objeto `user` retornado pelo `authorize` contém: id, name, email, role, cargo, tenantId, tenantSlug, tenantNome. Adicionar bloco `events` é o ponto de extensão.

Helper de trilha (src/lib/auditoria.ts:85-108): `gravarAuditoria` faz `logger.info(...)` + `prisma.<modelo>.create({ data: {...} })`, e cada gravação é envolvida em try/catch com `console.error` no catch — gravação de trilha NUNCA quebra o fluxo principal.

Data-layer filtrável (src/lib/data/auditorias.ts): `FiltrosAuditoria { entidade?, usuarioId?, acao?, pagina?, limite? }`; `listarAuditorias(tenantId, filtros)` monta `where` com spreads condicionais, faz `Promise.all([findMany, count])`, enriquece nome de usuário via `findMany({ where: { id: { in: ids } } })` + Map.

Página RSC filtrável análoga: src/app/(app)/almoxarifado/lotes/page.tsx — `searchParams: Promise<{...}>`, `await getTenant()`, filtros via `<form>` GET, componentes Card/CardHeader/Table/Badge de @/components/ui/\*, FadeIn de @/components/motion.

Enum EventoAcesso (criado no plano 11-01): LOGIN_SUCESSO, LOGIN_FALHA, LOGOUT, REFRESH_TOKEN, TOTP_OK, TOTP_FALHA, SENHA_RECUPERACAO_SOLICITADA, SENHA_RECUPERACAO_USADA.

RBAC: `requirePermissao(escopo, operacao)` de @/lib/permissoes — o escopo `auditoria` já existe e cobre esta tela.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helper registrarAcesso e data-layer filtrável</name>
  <files>src/lib/log-acesso.ts, src/lib/data/log-acesso.ts</files>
  <read_first>
    - src/lib/auditoria.ts (linhas 85-108 — gravarAuditoria; linhas 128-130 — catch tolerante)
    - src/lib/data/auditorias.ts (FiltrosAuditoria, listarAuditorias — where condicional, Promise.all, enriquecimento de nome)
    - src/lib/logger.ts (logger.info/warn/error)
    - .planning/research/PITFALLS.md (Pitfall 7 — LogAcesso refresh flood: NÃO registrar REFRESH_TOKEN a cada navegação)
  </read_first>
  <action>
    Criar `src/lib/log-acesso.ts` com `import "server-only"` no topo:
    - `async function registrarAcesso(params: { evento, tenantId, usuarioId?, ip?, userAgent?, sistema? })`. Faz `logger.info("log-acesso", { evento, usuarioId })` e `prisma.logAcesso.create({ data: {...} })`. TODA a gravação envolta em try/catch — no catch, `logger.error` e retorno silencioso (falha de log NUNCA bloqueia login/logout).
    - NÃO usar `prismaAuditado` — LogAcesso é a própria trilha; auditá-la gera loop (decisão CONTEXT.md / PATTERNS.md).
    - `evento` tipado pelo enum `EventoAcesso` gerado pelo Prisma.

    Criar `src/lib/data/log-acesso.ts` (camada de leitura, sem `server-only` — segue o padrão de data/auditorias.ts):
    - `interface FiltrosLogAcesso { usuarioId?, evento?, dataInicio?: Date, dataFim?: Date, pagina?, limite? }`.
    - `async function listarLogAcesso(tenantId, filtros = {})`: monta `where` com `tenantId` + spreads condicionais; filtro de período via `ocorreuEm: { gte: dataInicio, lt: dataFim }`; `Promise.all([findMany ordenado por ocorreuEm desc com skip/take, count])`. Enriquecer o nome do usuário com uma query `prisma.usuario.findMany({ where: { id: { in: ids } } })` + Map (espelhando auditorias.ts). Retornar `{ items, total }`.
    - Padrão de paginação: `limite = 100`, `skip = (pagina-1) * limite`.

  </action>
  <acceptance_criteria>
    - `grep -c "server-only" src/lib/log-acesso.ts` retorna 1
    - `grep -c "prismaAuditado" src/lib/log-acesso.ts` retorna 0 (LogAcesso não é auto-auditado)
    - `grep -c "try" src/lib/log-acesso.ts` retorna pelo menos 1 (gravação tolerante a erro)
    - `grep -E "export (async function|function) (registrarAcesso|listarLogAcesso)" src/lib/log-acesso.ts src/lib/data/log-acesso.ts` encontra as 2 exportações
    - `npx tsc --noEmit` exit code 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>registrarAcesso grava de forma tolerante a erro sem auto-auditar; listarLogAcesso filtra por usuário/evento/período com paginação e nome enriquecido.</done>
</task>

<task type="auto">
  <name>Task 2: Listeners de evento no NextAuth + captura de IP/user-agent no login</name>
  <files>src/auth.ts, src/lib/actions/auth.ts</files>
  <read_first>
    - src/auth.ts (estrutura do NextAuth({ ...authConfig, providers }) — o objeto user contém id, tenantId)
    - src/lib/actions/auth.ts (action de login atual — onde IP/user-agent podem ser capturados via headers())
    - .planning/phases/11-fundacao-infra-cruzada/11-PATTERNS.md (seção src/auth.ts — events.signIn/signOut, edge-safety)
    - .planning/research/PITFALLS.md (Pitfall 7 — distinguir signIn de refresh)
  </read_first>
  <action>
    Adicionar o bloco `events` ao objeto `NextAuth({...})` em `src/auth.ts`:
    - `events.signIn({ user })`: chama `registrarAcesso({ evento: "LOGIN_SUCESSO", usuarioId: user.id, tenantId: user.tenantId, ... })`.
    - `events.signOut(message)`: chama `registrarAcesso({ evento: "LOGOUT", ... })` — extrair usuarioId/tenantId do `message.token` (ou `message.session`) conforme a forma do payload do NextAuth v5.
    - NÃO registrar `REFRESH_TOKEN` no callback `jwt` — isso causaria explosão de linhas (Pitfall 7). O evento REFRESH_TOKEN fica disponível no enum para uso futuro deliberado, mas não é disparado a cada navegação.
    - Os listeners ficam em `auth.ts` (Node runtime), NUNCA em `auth.config.ts` (Edge-safe) — `registrarAcesso` toca Prisma.

    Em `src/lib/actions/auth.ts` (action de login): capturar IP e user-agent via `headers()` do Next.js (`x-forwarded-for`, `user-agent`) no fluxo de login. No caminho de falha de autenticação (credenciais inválidas), chamar `registrarAcesso({ evento: "LOGIN_FALHA", tenantId, usuarioId: undefined, ip, userAgent })` — LOGIN_FALHA é o que a LGPD mais pede. Como `events.signIn` não expõe IP/user-agent, repassar esses valores do contexto da action de login para a gravação de LOGIN_SUCESSO também (ou gravar LOGIN_SUCESSO direto na action após `signIn` bem-sucedido, em vez de no `events`, para ter IP — escolher a abordagem que garante IP presente e documentar no SUMMARY).

  </action>
  <acceptance_criteria>
    - `grep -c "events" src/auth.ts` retorna pelo menos 1
    - `grep -E "LOGIN_SUCESSO|LOGOUT" src/auth.ts src/lib/actions/auth.ts` encontra os dois eventos
    - `grep -c "LOGIN_FALHA" src/lib/actions/auth.ts` retorna pelo menos 1
    - `grep -c "REFRESH_TOKEN" src/auth.ts` retorna 0 (não disparado por navegação)
    - `npx tsc --noEmit` exit code 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Login bem-sucedido, login falho e logout geram registros em log_acesso com IP e user-agent; refresh de sessão não gera registro.</done>
</task>

<task type="auto">
  <name>Task 3: Tela administrativa de consulta do log de acessos</name>
  <files>src/app/(app)/auditoria/log-acesso/page.tsx</files>
  <read_first>
    - src/app/(app)/almoxarifado/lotes/page.tsx (página RSC filtrável — metadata, searchParams Promise, getTenant, form GET, Card/CardHeader/Table/Badge, FadeIn)
    - src/lib/data/log-acesso.ts (listarLogAcesso e FiltrosLogAcesso — criados na Task 1)
    - src/lib/permissoes.ts (requirePermissao — escopo auditoria já existe)
  </read_first>
  <action>
    Criar `src/app/(app)/auditoria/log-acesso/page.tsx` como Server Component, espelhando `lotes/page.tsx`:
    - `export const metadata` com title "Log de Acessos".
    - `searchParams: Promise<{ usuario?: string; evento?: string; inicio?: string; fim?: string; pagina?: string }>`.
    - No topo da função: `await requirePermissao("auditoria", "visualizar")` e `const tenant = await getTenant()`.
    - Ler os filtros de `searchParams`, converter `inicio`/`fim` para `Date`, chamar `listarLogAcesso(tenant.id, { usuarioId, evento, dataInicio, dataFim, pagina })`.
    - Renderizar dentro de `<FadeIn>`: um `Card` com `CardHeader` contendo um `<form>` GET com `<select name="evento">` (opções dos valores de EventoAcesso), `<input type="date" name="inicio">`, `<input type="date" name="fim">`, `<input name="usuario">` e botão Filtrar. Abaixo, uma `Table` (THead/TBody/TR/TH/TD) com colunas: data/hora, usuário, evento, IP, user-agent.
    - Usar `Badge` com tom de cor distinto para LOGIN_FALHA (tom perigo) vs LOGIN_SUCESSO (tom sucesso) — espelhar o padrão `toneLote` do análogo.
    - Filtros via `<form>` GET nativo + searchParams — NÃO usar `nuqs` (isso é UX-05, Fase 13).
  </action>
  <acceptance_criteria>
    - `grep -c "requirePermissao" src/app/(app)/auditoria/log-acesso/page.tsx` retorna 1
    - `grep -c "listarLogAcesso" src/app/(app)/auditoria/log-acesso/page.tsx` retorna 1
    - `grep -E "select|input" src/app/(app)/auditoria/log-acesso/page.tsx` encontra os campos de filtro
    - `npx tsc --noEmit` exit code 0
    - `npx next build` compila a rota sem erro (ou `npx eslint src/app/(app)/auditoria/log-acesso/page.tsx` sem erros se build for pesado)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>A tela /auditoria/log-acesso lista registros filtráveis por usuário, período e tipo de evento, protegida pelo escopo RBAC auditoria.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                |
| -------------------------- | ---------------------------------------------------------- |
| browser → action de login  | credenciais e IP/user-agent não-confiáveis cruzam aqui     |
| sessão admin → tela de log | leitura de trilha exige RBAC; expõe IPs de outros usuários |

## STRIDE Threat Register

| Threat ID | Category               | Component         | Disposition | Mitigation Plan                                                                                |
| --------- | ---------------------- | ----------------- | ----------- | ---------------------------------------------------------------------------------------------- |
| T-11-04   | Repudiation            | log de login      | mitigate    | LOGIN_SUCESSO, LOGIN_FALHA e LOGOUT gravados com IP/user-agent — trilha de não-repúdio         |
| T-11-05   | Information Disclosure | tela log-acesso   | mitigate    | `requirePermissao("auditoria","visualizar")` no topo da página; queries filtradas por tenantId |
| T-11-06   | Denial of Service      | tabela log_acesso | mitigate    | REFRESH_TOKEN não disparado por navegação (Pitfall 7); paginação na consulta                   |

</threat_model>

<verification>
- `npx tsc --noEmit` sem erros.
- Login real grava LOGIN_SUCESSO com IP; credencial inválida grava LOGIN_FALHA; logout grava LOGOUT.
- Navegar entre páginas autenticadas NÃO cria novos registros.
- A tela /auditoria/log-acesso aplica filtros e exige permissão de auditoria.
</verification>

<success_criteria>

- Cada login/logout/falha gera registro em log_acesso com IP e user-agent.
- Refresh de sessão não polui a tabela.
- Administrador consulta e filtra o log por usuário, período e tipo de evento.
  </success_criteria>

<output>
After completion, create `.planning/phases/11-fundacao-infra-cruzada/11-02-SUMMARY.md`
</output>
