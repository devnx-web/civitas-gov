---
phase: 11-fundacao-infra-cruzada
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - src/lib/auditoria.ts
  - src/lib/auditoria.test.ts
autonomous: true
requirements: [AUDIT-05]
must_haves:
  truths:
    - "Uma alteração em Empenho, Liquidacao, Pagamento, Aditamento, Ata ou Contrato gera um registro de auditoria"
    - "Esses registros entram na cadeia de hash já ativa (prevHash/currentHash preenchidos)"
    - "Campos pesados e sensíveis dos novos modelos são sanitizados antes de entrar na trilha"
  artifacts:
    - path: "src/lib/auditoria.ts"
      provides: "MODELOS_AUDITADOS estendido para cobrir as 6 entidades de B10"
      contains: "Ata"
    - path: "src/lib/auditoria.test.ts"
      provides: "Testes confirmando cobertura de auditoria das 6 entidades"
  key_links:
    - from: "src/lib/auditoria.ts"
      to: "MODELOS_AUDITADOS"
      via: "Set contém Empenho, Liquidacao, Pagamento, Aditamento, Ata, Contrato"
      pattern: '"Ata"'
---

<objective>
Implementar B10 — estender o whitelist `MODELOS_AUDITADOS` para que alterações em Empenho, Liquidação, Pagamento, Aditamento, Ata e Contrato entrem na trilha de auditoria.

Purpose: Cobertura de auditoria das 6 entidades financeiras/contratuais é exigência REQ-NF (AUDIT-05) e pré-requisito de AICHAT-03 (Fase 15 — detecção de inconsistências em empenhos/liquidações).

ORDEM HARD: este plano roda DEPOIS do plano 11-04 (hash chain ativo + constraint @unique). Estender os modelos auditados antes do hash chain deixaria as novas entradas permanentemente fora da cadeia.

Output: `MODELOS_AUDITADOS` estendido (apenas `Ata` é novo — os outros 5 já estão na lista); sanitização dos campos pesados; testes de cobertura.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-fundacao-infra-cruzada/11-CONTEXT.md
@.planning/phases/11-fundacao-infra-cruzada/11-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Padrões do codebase — extraídos dos arquivos canônicos. -->

src/lib/auditoria.ts:19-34 — `MODELOS_AUDITADOS` é um `Set` que JÁ contém: Usuario, Fornecedor, Material, Contrato, Aditamento, Empenho, Liquidacao, Pagamento, BemPatrimonial, ProcessoLicitatorio, Configuracao, Permissao, RolePermissao, UsuarioPermissao. Das 6 entidades-alvo de B10 (Empenho, Liquidacao, Pagamento, Aditamento, Ata, Contrato), apenas `Ata` AINDA NÃO está na lista — os outros 5 já estão (confirmado por PATTERNS.md e ARCHITECTURE.md).

src/lib/auditoria.ts:53-69 — `SANITIZAR` é um `Record<string, (d) => Campos>` que transforma os dados antes de gravar (ex. remove senhaHash de Usuario, mascara CPF de Fornecedor). Modelos como Ata/Contrato/Aditamento têm campos `Json` (cláusulas, anexos, histórico) que podem ser grandes (Pitfall 13 — audit Json gigante).

A extensão Prisma `prismaAuditado` (linhas 112-214) já intercepta create/update/delete/upsert e grava via `gravarAuditoria` — que após o plano 11-04 já computa o hash chain. Adicionar um modelo ao Set é suficiente para ele entrar na trilha encadeada.

Vitest configurado; estilo de testes existente cobre helpers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender MODELOS_AUDITADOS e sanitizar campos pesados</name>
  <files>src/lib/auditoria.ts</files>
  <read_first>
    - src/lib/auditoria.ts (MODELOS_AUDITADOS linhas 19-34; SANITIZAR linhas 53-69; sanitizar linhas 65-69)
    - .planning/phases/11-fundacao-infra-cruzada/11-PATTERNS.md (seção src/lib/auditoria.ts — confirmar que só Ata falta)
    - .planning/research/PITFALLS.md (Pitfall 13 — audit Json gigante: SANITIZAR profundo para Ata/Contrato/Aditamento, excluir campos Json pesados/redundantes)
  </read_first>
  <action>
    Verificar primeiro o conteúdo atual de `MODELOS_AUDITADOS`. Confirmar que Empenho, Liquidacao, Pagamento, Aditamento e Contrato já estão presentes. Adicionar o que faltar — pelo PATTERNS.md, apenas `"Ata"` é novo. Adicionar `"Ata"` ao Set com um comentário `// B10 Fase 11`.

    NÃO adicionar modelos de fases futuras (TicketSuporte, Webhook, ApiKey, etc. — esses são Fases 12/15). O escopo de B10 nesta fase é exatamente as 6 entidades de AUDIT-05.

    Em `SANITIZAR`: adicionar entradas para `Ata`, `Contrato` e `Aditamento` que removam ou compactem campos `Json` pesados (cláusulas extensas, metadados de anexos, histórico de revisões) — manter na trilha apenas o essencial para o diff antes/depois, evitando entradas de 20-50KB (Pitfall 13). Se um campo Json for indispensável ao diff, mantê-lo; se for redundante/volumoso, omiti-lo da gravação. Documentar a escolha no SUMMARY.

  </action>
  <acceptance_criteria>
    - `grep -c "\"Ata\"" src/lib/auditoria.ts` retorna pelo menos 1
    - Todas as 6 entidades presentes: `for m in Empenho Liquidacao Pagamento Aditamento Ata Contrato; do grep -q "\"$m\"" src/lib/auditoria.ts || echo "FALTA $m"; done` não imprime nada
    - `grep -E "Ata:|Contrato:|Aditamento:" src/lib/auditoria.ts` encontra entradas de sanitização para os modelos com Json pesado
    - `npx tsc --noEmit` exit code 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>MODELOS_AUDITADOS cobre as 6 entidades de B10; campos Json pesados de Ata/Contrato/Aditamento são sanitizados.</done>
</task>

<task type="auto">
  <name>Task 2: Testes de cobertura de auditoria das 6 entidades</name>
  <files>src/lib/auditoria.test.ts</files>
  <read_first>
    - src/lib/auditoria.ts (MODELOS_AUDITADOS — para o teste verificar a presença)
    - vitest.config.ts
    - src/lib/auditoria-hash.test.ts (estilo de teste — criado no plano 11-04)
  </read_first>
  <action>
    Criar (ou estender) `src/lib/auditoria.test.ts` com testes que:
    - Verificam que cada uma das 6 entidades (Empenho, Liquidacao, Pagamento, Aditamento, Ata, Contrato) está em `MODELOS_AUDITADOS` — exportar o Set ou um helper `modeloEhAuditado(nome)` de auditoria.ts para tornar isso testável (adicionar a exportação se não existir).
    - Verificam que a função `sanitizar` aplica a transformação esperada para Ata/Contrato/Aditamento — dado um objeto com um campo Json pesado, o resultado sanitizado o omite/compacta.
    - NÃO testar a integração com banco aqui (cobertura de gravação real fica para verificação manual/E2E) — testes são unitários sobre a configuração e a sanitização.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/auditoria.test.ts` passa
    - O teste cobre as 6 entidades — `grep -E "Empenho|Liquidacao|Pagamento|Aditamento|Ata|Contrato" src/lib/auditoria.test.ts` encontra as 6
    - `npx vitest run` mantém toda a suíte verde
    - `npx tsc --noEmit` exit code 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/auditoria.test.ts</automated>
  </verify>
  <done>Testes confirmam que as 6 entidades de B10 estão auditadas e que a sanitização compacta os campos Json pesados.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                 |
| ------------------------------------------ | ----------------------------------------------------------- |
| mutação de Empenho/Liquidacao/etc → trilha | alterações financeiras/contratuais precisam de não-repúdio  |
| dados Json grandes → tabela auditorias     | payloads de 20-50KB inflam a tabela e degradam o hash chain |

## STRIDE Threat Register

| Threat ID | Category          | Component                                            | Disposition | Mitigation Plan                                                                                                                |
| --------- | ----------------- | ---------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------ |
| T-11-16   | Repudiation       | Empenho/Liquidacao/Pagamento/Aditamento/Ata/Contrato | mitigate    | As 6 entidades em MODELOS_AUDITADOS — toda alteração gera registro encadeado no hash chain (B4 já ativo)                       |
| T-11-17   | Tampering         | ordem B10 após B4                                    | mitigate    | depends_on: [04] — B10 só roda com hash chain ativo e constraint @unique aplicada; novas entradas entram na cadeia (Pitfall 4) |
| T-11-18   | Denial of Service | tabela auditorias                                    | mitigate    | SANITIZAR compacta campos Json pesados de Ata/Contrato/Aditamento (Pitfall 13)                                                 |

</threat_model>

<verification>
- `npx vitest run` — toda a suíte verde, incluindo os testes de cobertura de B10.
- `npx tsc --noEmit` sem erros.
- Uma alteração real em qualquer das 6 entidades gera registro de auditoria com prevHash/currentHash preenchidos (verificação manual/E2E).
</verification>

<success_criteria>

- Alterações em Empenho, Liquidação, Pagamento, Aditamento, Ata e Contrato geram registros de auditoria.
- Os registros entram na cadeia de hash já ativa.
- Campos Json pesados são sanitizados antes da gravação.
  </success_criteria>

<output>
After completion, create `.planning/phases/11-fundacao-infra-cruzada/11-05-SUMMARY.md`
</output>
