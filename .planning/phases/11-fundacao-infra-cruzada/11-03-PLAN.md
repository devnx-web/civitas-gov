---
phase: 11-fundacao-infra-cruzada
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/lib/notificacoes/index.ts
  - src/lib/notificacoes/dispatcher.ts
  - src/lib/data/notificacoes.ts
  - src/lib/actions/notificacoes.ts
  - src/app/api/notificacoes/route.ts
  - src/app/api/notificacoes/marcar-lida/route.ts
  - src/components/notificacoes/SinoNotificacoes.tsx
  - src/components/notificacoes/ListaNotificacoes.tsx
  - src/components/layout/topbar.tsx
  - src/app/(app)/configuracoes/notificacoes/page.tsx
autonomous: true
requirements: [NOTIF-01, NOTIF-02, NOTIF-03, NOTIF-04, NOTIF-05]
must_haves:
  truths:
    - "O usuário vê no sino da topbar o contador de notificações não lidas, atualizado por polling"
    - "O usuário abre o painel dropdown e vê as últimas notificações com horário relativo (<24h) ou data"
    - "O usuário marca uma notificação como lida individualmente ou todas de uma vez"
    - "O usuário acessa uma tela de preferências e escolhe quais categorias de notificação deseja receber"
    - "O sistema gera notificações por eventos via o dispatcher, respeitando a preferência de categoria do usuário"
  artifacts:
    - path: "src/lib/notificacoes/dispatcher.ts"
      provides: "Serviço que gera notificações por evento respeitando preferências"
      exports: ["notificar"]
    - path: "src/components/notificacoes/SinoNotificacoes.tsx"
      provides: "Sino funcional com contador e painel dropdown (client component, polling)"
    - path: "src/app/api/notificacoes/route.ts"
      provides: "Endpoint autenticado de leitura para polling do sino"
      exports: ["GET"]
    - path: "src/app/(app)/configuracoes/notificacoes/page.tsx"
      provides: "Tela de preferências de categoria de notificação"
  key_links:
    - from: "src/components/layout/topbar.tsx"
      to: "src/components/notificacoes/SinoNotificacoes.tsx"
      via: "substituição do botão sino decorativo pelo componente funcional"
      pattern: "SinoNotificacoes"
    - from: "src/components/notificacoes/SinoNotificacoes.tsx"
      to: "/api/notificacoes"
      via: "fetch em setInterval de 30s (polling)"
      pattern: "api/notificacoes"
    - from: "src/lib/notificacoes/dispatcher.ts"
      to: "prisma.notificacao"
      via: "create após checar PreferenciaNotificacao"
      pattern: "prisma\\.notificacao\\.create"
---

<objective>
Implementar B8 — a central de notificações: sino funcional na topbar com contador de não lidas, painel dropdown, marcação individual/em massa, preferências de categoria e o dispatcher que gera notificações por eventos do sistema.

Purpose: O sino é o hub de UX cross-feature do milestone — relatórios prontos, chamados, contratos a vencer e documentos a assinar (Fases 12 e 15) notificam o usuário por ele. Entregar funcional, não decorativo.

Output: serviço de notificações + dispatcher, data-layer, server actions, endpoints de polling, componentes do sino e painel, tela de preferências.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-fundacao-infra-cruzada/11-CONTEXT.md
@.planning/phases/11-fundacao-infra-cruzada/11-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Padrões do codebase — extraídos dos arquivos canônicos. -->

Server Actions (src/lib/actions.ts): `defineAction(schema, handler)` e `defineFormAction` retornam `Resultado<T>` (`{ ok, data?, erro?, campos? }`); `AppError("mensagem segura")` para erro de domínio. PREFERIR `defineAction` sobre o estilo legado de lotes.ts.

Multi-tenancy + RBAC: `getTenant()` de @/lib/tenant; `requirePermissao(escopo, operacao)` de @/lib/permissoes. O escopo `notificacoes` foi adicionado ao enum Escopo no plano 11-01. Notificações são escopadas por `usuarioId` — cada usuário só vê e marca as suas; `where` sempre com `tenantId` + `usuarioId` (dupla checagem).

Route handler autenticado: análogo src/app/api/esic/route.ts (GET/POST + Zod + erros tipados 422/500), MAS /api/notificacoes é autenticada — usar `auth()` de @/auth para resolver `session.user.id` e `tenantId` (não `resolverTenant` por slug). GET de polling responde com header `Cache-Control: no-store`.

Sino atual decorativo (src/components/layout/topbar.tsx): botão com `<Bell className="h-5 w-5" />` e badge estático — importado de lucide-react. Substituir pelo componente funcional.

Padrão de dropdown da topbar: `AnimatePresence` + `motion.div` (framer-motion) com overlay `fixed inset-0` para fechar ao clicar fora; transição duration 0.16.

Enum CategoriaNotificacao (criado no plano 11-01): CHAMADO, CONTRATO, RELATORIO, DOCUMENTO, AUDITORIA, SISTEMA.

Modelos (plano 11-01): Notificacao { tenantId, usuarioId, categoria, titulo, corpo, link?, lida, lidaEm?, criadoEm }; PreferenciaNotificacao { tenantId, usuarioId, categoria, habilitada } com @@unique([tenantId, usuarioId, categoria]).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serviço de notificações, dispatcher e data-layer</name>
  <files>src/lib/notificacoes/index.ts, src/lib/notificacoes/dispatcher.ts, src/lib/data/notificacoes.ts</files>
  <read_first>
    - src/lib/auditoria.ts (linha 1 — server-only; comAuditoria como orquestrador de evento)
    - src/lib/data/auditorias.ts (padrão de listagem com where condicional e Promise.all)
    - src/lib/logger.ts
    - .planning/research/PITFALLS.md (Pitfall 8 — notificações em loop síncrono: para geração em massa, oferecer caminho via pg-boss; Pitfall 13 não se aplica aqui)
    - .planning/phases/11-fundacao-infra-cruzada/11-PATTERNS.md (seções notificacoes/index.ts, dispatcher.ts, data/log-acesso.ts)
  </read_first>
  <action>
    Criar `src/lib/notificacoes/index.ts` com `import "server-only"`: funções CRUD de serviço — `criarNotificacao({ tenantId, usuarioId, categoria, titulo, corpo, link? })` que faz `prisma.notificacao.create`; `marcarLida(tenantId, usuarioId, notificacaoId)` e `marcarTodasLidas(tenantId, usuarioId)` que setam `lida: true, lidaEm: new Date()` com `where` incluindo tenantId+usuarioId; `contarNaoLidas(tenantId, usuarioId)`. NÃO usar `prismaAuditado` (volume alto, nada sensível — PATTERNS.md).

    Criar `src/lib/notificacoes/dispatcher.ts` com `import "server-only"`: `async function notificar({ tenantId, usuarioId, categoria, titulo, corpo, link? })` — antes de criar a notificação, consultar `PreferenciaNotificacao` do par (tenantId, usuarioId, categoria); se existir registro com `habilitada: false`, NÃO criar (opt-out); se não existir registro, criar (default habilitada). Para geração em lote (vários usuários), expor `notificarEmLote(...)` que enfileira via pg-boss (`getBoss()` de @/lib/jobs/boss) em vez de loop síncrono — evita Pitfall 8. O registro de um handler de job de notificação no worker é opcional nesta fase; o caminho síncrono `notificar` é o mínimo entregável.

    Criar `src/lib/data/notificacoes.ts` (camada de leitura): `listarNotificacoes(tenantId, usuarioId, { apenasNaoLidas?, limite = 20 })` — `findMany` ordenado por `criadoEm desc`, `where` com tenantId+usuarioId. Retornar os campos necessários ao painel.

  </action>
  <acceptance_criteria>
    - `grep -lc "server-only" src/lib/notificacoes/index.ts src/lib/notificacoes/dispatcher.ts` lista os 2 arquivos
    - `grep -c "PreferenciaNotificacao\|preferenciaNotificacao" src/lib/notificacoes/dispatcher.ts` retorna pelo menos 1 (checagem de preferência)
    - `grep -E "export (async function|function) (notificar|criarNotificacao|listarNotificacoes)" src/lib/notificacoes/index.ts src/lib/notificacoes/dispatcher.ts src/lib/data/notificacoes.ts` encontra as funções
    - `grep -c "prismaAuditado" src/lib/notificacoes/index.ts` retorna 0
    - `npx tsc --noEmit` exit code 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>O dispatcher gera notificações respeitando preferências de categoria; o serviço marca lidas e conta não lidas filtrando por tenant+usuário.</done>
</task>

<task type="auto">
  <name>Task 2: Server actions, endpoints de polling e tela de preferências</name>
  <files>src/lib/actions/notificacoes.ts, src/app/api/notificacoes/route.ts, src/app/api/notificacoes/marcar-lida/route.ts, src/app/(app)/configuracoes/notificacoes/page.tsx</files>
  <read_first>
    - src/lib/actions.ts (defineAction, AppError, Resultado)
    - src/app/api/esic/route.ts (route handler GET/POST + Zod + erros tipados)
    - src/lib/actions/lotes.ts (estrutura de action — para comparar; preferir defineAction)
    - src/app/(app)/almoxarifado/lotes/page.tsx (página RSC — para a tela de preferências)
    - .planning/research/PITFALLS.md (Pitfall 9 — polling sem dedupe; o GET deve ser idempotente e barato)
  </read_first>
  <action>
    Criar `src/lib/actions/notificacoes.ts`: usar `defineAction` de @/lib/actions. Action `marcarLidaAction(schema { id: cuid })` — resolve tenant+usuário via `auth()`/`getTenant()`, chama `marcarLida`. Action `marcarTodasLidasAction`. Action `salvarPreferenciasAction(schema)` — recebe o mapa categoria→habilitada e faz `upsert` em PreferenciaNotificacao por categoria (`where` no @@unique composto). Toda mutação chama `requirePermissao("notificacoes", "editar")` antes.

    Criar `src/app/api/notificacoes/route.ts` — `GET` autenticado: resolve `session` via `auth()`, retorna `{ naoLidas: number, itens: [...] }` (últimas ~20 via `listarNotificacoes`). Header `Cache-Control: no-store`. Query sempre filtrada por tenantId+usuarioId da sessão. Sem sessão → 401.

    Criar `src/app/api/notificacoes/marcar-lida/route.ts` — `POST` autenticado: body Zod `{ id?: string, todas?: boolean }`; valida; chama `marcarLida` ou `marcarTodasLidas`; 422 em body inválido, 500 em erro interno (espelhar esic/route.ts).

    Criar `src/app/(app)/configuracoes/notificacoes/page.tsx` — Server Component: `requirePermissao("notificacoes", "visualizar")`, `getTenant()`, carrega as preferências atuais do usuário (`PreferenciaNotificacao` por categoria, default habilitada), renderiza dentro de `<FadeIn>` um `Card` com um `<form>` que submete a `salvarPreferenciasAction` — um toggle/checkbox por valor de `CategoriaNotificacao`.

  </action>
  <acceptance_criteria>
    - `grep -c "defineAction" src/lib/actions/notificacoes.ts` retorna pelo menos 1
    - `grep -c "requirePermissao" src/lib/actions/notificacoes.ts` retorna pelo menos 1
    - `grep -c "no-store" src/app/api/notificacoes/route.ts` retorna 1
    - `grep -E "auth\(\)" src/app/api/notificacoes/route.ts src/app/api/notificacoes/marcar-lida/route.ts` encontra a resolução de sessão nos dois handlers
    - `grep -c "CategoriaNotificacao\|categoria" src/app/(app)/configuracoes/notificacoes/page.tsx` retorna pelo menos 1
    - `npx tsc --noEmit` exit code 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>As actions marcam lidas e salvam preferências; o endpoint GET serve o polling autenticado com no-store; a tela de preferências persiste opt-in por categoria.</done>
</task>

<task type="auto">
  <name>Task 3: Sino funcional na topbar com painel dropdown e polling</name>
  <files>src/components/notificacoes/SinoNotificacoes.tsx, src/components/notificacoes/ListaNotificacoes.tsx, src/components/layout/topbar.tsx</files>
  <read_first>
    - src/components/layout/topbar.tsx (botão sino decorativo linhas ~58-68; padrão de dropdown AnimatePresence + motion.div + overlay)
    - .planning/research/PITFALLS.md (Pitfall 9 — polling sem dedupe causa cascading state-set: usar cleanup do interval e abort de fetch, comparar antes de setState)
    - .planning/phases/11-fundacao-infra-cruzada/11-PATTERNS.md (seções SinoNotificacoes.tsx, ListaNotificacoes.tsx)
  </read_first>
  <action>
    Criar `src/components/notificacoes/SinoNotificacoes.tsx` com `"use client"`: botão sino (`Bell` de lucide-react) com badge de contador — o badge só aparece se `naoLidas > 0`, mostrando o número. Estado `aberto` controla o painel. `useEffect` com `setInterval(30000)` faz `fetch("/api/notificacoes")` e atualiza o contador; OBRIGATÓRIO: `clearInterval` no cleanup do `useEffect` e usar `AbortController` para cancelar fetch pendente; comparar o valor recebido antes de `setState` para evitar re-render desnecessário (Pitfall 9). Recebe contagem/lista iniciais via props do RSC (topbar) para evitar flash. Painel via `AnimatePresence` + `motion.div` + overlay `fixed inset-0` espelhando o dropdown de usuário existente.

    Criar `src/components/notificacoes/ListaNotificacoes.tsx` com `"use client"`: renderiza as últimas notificações — para cada uma: título, corpo, horário relativo (`Intl.RelativeTimeFormat` em pt-BR para <24h, senão data formatada via `Intl.DateTimeFormat`), link clicável (deep link interno) e ação "marcar como lida" individual; cabeçalho com botão "marcar todas como lidas". Ações chamam os endpoints/actions criados na Task 2 e revalidam o contador.

    Modificar `src/components/layout/topbar.tsx`: substituir o botão `<Bell>` decorativo (linhas ~58-68) por `<SinoNotificacoes />`. Como a topbar é Server Component, buscar a contagem inicial via `contarNaoLidas` e passar como prop ao `SinoNotificacoes`. Não quebrar o restante da topbar (menu de usuário etc.).

  </action>
  <acceptance_criteria>
    - `grep -lc "use client" src/components/notificacoes/SinoNotificacoes.tsx src/components/notificacoes/ListaNotificacoes.tsx` lista os 2 arquivos
    - `grep -c "SinoNotificacoes" src/components/layout/topbar.tsx` retorna pelo menos 1 (sino funcional importado)
    - `grep -E "clearInterval|AbortController" src/components/notificacoes/SinoNotificacoes.tsx` encontra o cleanup do polling
    - `grep -c "RelativeTimeFormat\|DateTimeFormat" src/components/notificacoes/ListaNotificacoes.tsx` retorna pelo menos 1 (horário relativo)
    - `npx tsc --noEmit` exit code 0
    - `npx next build` compila sem erro (ou `npx eslint src/components/notificacoes/ src/components/layout/topbar.tsx` sem erros)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>O sino mostra o contador real de não lidas e atualiza por polling sem vazamento de interval; o painel lista notificações com horário relativo e permite marcar lida individual/em massa.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                 |
| -------------------------------- | ----------------------------------------------------------- |
| browser → /api/notificacoes      | polling não-autenticado tentaria ler notificações de outros |
| client component → server action | id de notificação fornecido pelo cliente é não-confiável    |

## STRIDE Threat Register

| Threat ID | Category               | Component            | Disposition | Mitigation Plan                                                                             |
| --------- | ---------------------- | -------------------- | ----------- | ------------------------------------------------------------------------------------------- |
| T-11-07   | Information Disclosure | /api/notificacoes    | mitigate    | Endpoint resolve sessão via `auth()`; query filtrada por tenantId+usuarioId; 401 sem sessão |
| T-11-08   | Tampering              | marcarLidaAction     | mitigate    | `where` com tenantId+usuarioId — usuário só marca as próprias notificações                  |
| T-11-09   | Denial of Service      | polling do sino      | mitigate    | Interval limpo no cleanup + AbortController (Pitfall 9); GET barato com no-store            |
| T-11-10   | Elevation of Privilege | tela de preferências | mitigate    | `requirePermissao("notificacoes", ...)` nas actions e na página                             |

</threat_model>

<verification>
- `npx tsc --noEmit` sem erros.
- O sino exibe contador real; abrir o painel mostra notificações com horário relativo.
- Marcar lida (individual e em massa) atualiza o contador.
- Salvar preferências com uma categoria desabilitada faz o dispatcher não criar notificações dessa categoria.
</verification>

<success_criteria>

- Sino na topbar com contador de não lidas atualizado por polling.
- Painel com últimas notificações, horário relativo, marcação individual e em massa.
- Tela de preferências persiste opt-in por categoria.
- Dispatcher gera notificações por evento respeitando as preferências.
  </success_criteria>

<output>
After completion, create `.planning/phases/11-fundacao-infra-cruzada/11-03-SUMMARY.md`
</output>
