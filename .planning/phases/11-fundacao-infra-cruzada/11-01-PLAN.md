---
phase: 11-fundacao-infra-cruzada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260520000000_v05_fundacao/migration.sql
  - src/lib/jobs/boss.ts
  - src/lib/jobs/worker.ts
  - src/lib/jobs/handlers/heartbeat.ts
  - package.json
  - .env.example
autonomous: false
requirements:
  [
    AUDIT-01,
    AUDIT-02,
    AUDIT-03,
    AUDIT-04,
    AUDIT-05,
    NOTIF-01,
    NOTIF-02,
    NOTIF-03,
    NOTIF-04,
    NOTIF-05,
  ]
must_haves:
  truths:
    - "A migração v05_fundacao está aplicada ao banco — as tabelas log_acesso, notificacoes, preferencias_notificacao existem e a tabela auditorias tem colunas prevHash/currentHash"
    - "O enum Escopo do banco contém os 8 valores novos do milestone v0.5 (notificacoes, jobs, helpdesk, tce_es, webhooks, api, bi, ia) além dos 12 existentes"
    - "O comando pnpm jobs:worker inicia um processo separado que conecta ao pg-boss e permanece vivo"
    - "O pg-boss cria suas tabelas no schema pgboss (isolado de public)"
  artifacts:
    - path: "prisma/migrations/20260520000000_v05_fundacao/migration.sql"
      provides: "DDL aditiva: 3 tabelas novas, colunas em auditorias, enums novos, valores de Escopo"
      contains: 'CREATE TABLE "log_acesso"'
    - path: "src/lib/jobs/boss.ts"
      provides: "Singleton de conexão pg-boss"
      exports: ["getBoss"]
    - path: "src/lib/jobs/worker.ts"
      provides: "Entry-point do processo worker separado"
    - path: "prisma/schema.prisma"
      provides: "Modelos LogAcesso, Notificacao, PreferenciaNotificacao; colunas em Auditoria; enums novos"
      contains: "model LogAcesso"
  key_links:
    - from: "src/lib/jobs/worker.ts"
      to: "src/lib/jobs/boss.ts"
      via: "import do singleton getBoss"
      pattern: "getBoss"
    - from: "package.json"
      to: "src/lib/jobs/worker.ts"
      via: "script jobs:worker"
      pattern: "jobs:worker"
---

<objective>
Estabelecer a fundação física do milestone v0.5: a migração consolidada `v05_fundacao` (todo o schema novo da Fase 11, aditivo) e a fila de jobs `pg-boss` com worker em processo separado.

Purpose: Sem o schema migrado, build e tipos passam mas o banco não tem as tabelas (falso-positivo). Sem pg-boss, nenhuma feature agendada do milestone começa. Este plano é a raiz da ordem hard da fase — todos os planos seguintes dependem dele.

Output: schema.prisma atualizado; migração `v05_fundacao` aplicada; `pg-boss` instalado com singleton e worker; script `jobs:worker` em package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-fundacao-infra-cruzada/11-CONTEXT.md
@.planning/phases/11-fundacao-infra-cruzada/11-PATTERNS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Padrões do codebase que o executor deve seguir — extraídos dos arquivos canônicos. -->

Singleton resistente a hot-reload (src/lib/prisma.ts):

```typescript
const globalForPrisma = globalThis as unknown as { prisma?: PrismaClient };
const adapter = new PrismaPg({ connectionString: process.env.DATABASE_URL });
export const prisma = globalForPrisma.prisma ?? new PrismaClient({ adapter });
if (process.env.NODE_ENV !== "production") {
  globalForPrisma.prisma = prisma;
}
```

Modelo de referência Auditoria (prisma/schema.prisma:130-146) e enum Escopo (151-164) — os 12 valores atuais de Escopo: licitacoes, contratos, fornecedores, almoxarifado, patrimonio, transparencia, configuracoes, usuarios, auditoria, relatorios, orcamento, financeiro.

Bloco scripts atual de package.json: dev, build, start, lint, postinstall, db:seed (tsx prisma/seed.ts), db:reset. O `tsx` (^4.22.2) já é devDependency.

Migração análoga: prisma/migrations/20260519080000_2fa_lotes_ropa/migration.sql — cabeçalho de comentário, ALTER TABLE aditivo, CREATE TYPE para enums, CREATE TABLE multi-tenant com índice e FK CASCADE.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modelar schema v05 — modelos, colunas e enums novos</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (modelo Auditoria linhas 130-146, enum Escopo linhas 151-164, modelo Tenant linhas 21-78, modelo Usuario linhas 81-102 — replicar o padrão tenantId/FK CASCADE/@@index/@@map)
    - .planning/phases/11-fundacao-infra-cruzada/11-PATTERNS.md (seção "prisma/schema.prisma" — convenções snake_case plural, camelCase, id TEXT cuid)
    - .planning/research/STACK.md (seção 1.4 — formato do modelo Notificacao)
  </read_first>
  <action>
    Adicionar ao prisma/schema.prisma, todos aditivos (nada removido):

    (a) Enum `EventoAcesso` com valores: LOGIN_SUCESSO, LOGIN_FALHA, LOGOUT, REFRESH_TOKEN, TOTP_OK, TOTP_FALHA, SENHA_RECUPERACAO_SOLICITADA, SENHA_RECUPERACAO_USADA.

    (b) Enum `CategoriaNotificacao` com valores: CHAMADO, CONTRATO, RELATORIO, DOCUMENTO, AUDITORIA, SISTEMA.

    (c) Modelo `LogAcesso`: id String @id @default(cuid()); tenantId String; usuarioId String? (nullable — login falho pode não ter usuário resolvido); evento EventoAcesso; ip String?; userAgent String?; sistema String? (módulo acessado); ocorreuEm DateTime @default(now()). Relação tenant Tenant FK onDelete Cascade. `@@index([tenantId, ocorreuEm(sort: Desc)])`, `@@index([tenantId, usuarioId])`, `@@index([tenantId, evento])`. `@@map("log_acesso")`.

    (d) Modelo `Notificacao`: id; tenantId String; usuarioId String; categoria CategoriaNotificacao; titulo String; corpo String; link String?; lida Boolean @default(false); lidaEm DateTime?; criadoEm DateTime @default(now()). Relação tenant FK onDelete Cascade. `@@index([tenantId, usuarioId, lida])`, `@@index([tenantId, criadoEm(sort: Desc)])`. `@@map("notificacoes")`.

    (e) Modelo `PreferenciaNotificacao`: id; tenantId String; usuarioId String; categoria CategoriaNotificacao; habilitada Boolean @default(true). Relação tenant FK onDelete Cascade. `@@unique([tenantId, usuarioId, categoria])`. `@@map("preferencias_notificacao")`.

    (f) No modelo `Auditoria`: adicionar `prevHash String?` e `currentHash String?` — AMBOS nullable, SEM `@unique` (a constraint vem no plano 11-04 após backfill, conforme ordem hard B4).

    (g) No enum `Escopo`: adicionar ao final os 8 valores do milestone v0.5 numa migração aditiva única — `notificacoes`, `jobs`, `helpdesk`, `tce_es`, `webhooks`, `api`, `bi`, `ia`. Razão: `ALTER TYPE "Escopo" ADD VALUE` no Postgres não pode rodar dentro de transação com uso imediato do valor; consolidar os 8 na fundação evita 3 migrações `ALTER TYPE` futuras nas Fases 12/15. Apenas `notificacoes` e `jobs` serão referenciados em RBAC nesta fase; os outros 6 ficam declarados aguardando as fases futuras.

    Adicionar os back-relations `logAcesso LogAcesso[]`, `notificacoes Notificacao[]`, `preferenciasNotificacao PreferenciaNotificacao[]` ao modelo Tenant.

  </action>
  <acceptance_criteria>
    - `grep -c "model LogAcesso\|model Notificacao\|model PreferenciaNotificacao" prisma/schema.prisma` retorna 3
    - `grep -c "prevHash\|currentHash" prisma/schema.prisma` retorna 2 e nenhuma das duas linhas contém `@unique`
    - `grep -cE "^\s+(notificacoes|jobs|helpdesk|tce_es|webhooks|api|bi|ia)$" prisma/schema.prisma` retorna 8 — os 8 valores novos no enum Escopo
    - `npx prisma validate` termina com exit code 0
  </acceptance_criteria>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>schema.prisma valida; 3 modelos novos, 2 colunas nullable em Auditoria, 2 enums novos e 8 valores de Escopo presentes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 [BLOCKING]: Aplicar a migração v05_fundacao ao banco</name>
  <files>prisma/migrations/20260520000000_v05_fundacao/migration.sql</files>
  <read_first>
    - prisma/migrations/20260519080000_2fa_lotes_ropa/migration.sql (cabeçalho, ALTER aditivo, CREATE TYPE, CREATE TABLE + índice + FK CASCADE)
    - .planning/research/PITFALLS.md (Pitfall 3 — comAuditoria bypassado: garantir que a migração não dispara nada fora de trilha; Pitfall 4 — ordem B4 antes de B10)
  </read_first>
  <action>
    Gerar e aplicar a migração consolidada `v05_fundacao`. Rodar `npx prisma migrate dev --name v05_fundacao` — isso cria o diretório `prisma/migrations/20260520000000_v05_fundacao/` (timestamp real gerado pelo Prisma), aplica o DDL ao banco e regenera o Prisma Client.

    Se `migrate dev` não estiver disponível no ambiente (banco gerenciado/sem shadow DB), usar `npx prisma db push` e em seguida criar manualmente o diretório de migração com `migration.sql` espelhando o DDL aplicado, seguindo o cabeçalho de comentário do análogo (`-- v05_fundacao: pg-boss schema, LogAcesso, Notificacao, hash chain columns`).

    A migração DEVE ser 100% aditiva: colunas nullable, novos CREATE TYPE/TABLE, `ALTER TYPE "Escopo" ADD VALUE`. Nenhuma coluna existente é removida ou alterada de forma destrutiva.

    NÃO criar a constraint `@unique` em `currentHash` — isso é o plano 11-04, após o backfill.

  </action>
  <acceptance_criteria>
    - Comando de verificação confirma que as tabelas existem no banco: `psql "$DATABASE_URL" -tAc "SELECT to_regclass('public.log_acesso'), to_regclass('public.notificacoes'), to_regclass('public.preferencias_notificacao')"` retorna 3 nomes não-nulos
    - `psql "$DATABASE_URL" -tAc "SELECT column_name FROM information_schema.columns WHERE table_name='auditorias' AND column_name IN ('prevHash','currentHash')"` retorna 2 linhas
    - `npx prisma migrate status` reporta a migração v05_fundacao como aplicada (ou `prisma db pull` confirma o schema sincronizado)
    - Diretório `prisma/migrations/*v05_fundacao/migration.sql` existe
  </acceptance_criteria>
  <verify>
    <automated>npx prisma migrate status</automated>
  </verify>
  <done>Migração v05_fundacao aplicada; as 3 tabelas e as colunas de hash existem fisicamente no banco; Prisma Client regenerado.</done>
</task>

<task type="auto">
  <name>Task 3: Instalar pg-boss e criar singleton + worker em processo separado</name>
  <files>src/lib/jobs/boss.ts, src/lib/jobs/worker.ts, src/lib/jobs/handlers/heartbeat.ts, package.json, .env.example</files>
  <read_first>
    - src/lib/prisma.ts (padrão de singleton resistente a hot-reload)
    - .planning/research/STACK.md (seção 1.1 — pg-boss ^10.1.0, mesma DATABASE_URL, worker em processo separado)
    - .planning/research/PITFALLS.md (Pitfall 5 — worker dentro do processo Next.js; Pitfall 6 — schema pgboss explícito)
    - .planning/phases/11-fundacao-infra-cruzada/11-PATTERNS.md (seções boss.ts e worker.ts)
  </read_first>
  <action>
    Instalar `pg-boss@^10.1.0` como dependency (`pnpm add pg-boss`).

    Criar `src/lib/jobs/boss.ts` com `import "server-only"` no topo: singleton de `PgBoss` espelhando o padrão de `prisma.ts` — `globalThis.boss ?? new PgBoss({ connectionString: process.env.DATABASE_URL, schema: "pgboss" })`. O `schema: "pgboss"` é OBRIGATÓRIO e explícito (Pitfall 6 — sem isso as tabelas poluem o schema public). Exportar `async function getBoss()` que faz lazy `boss.start()` na primeira chamada e retorna a instância iniciada. Usar `logger` de `@/lib/logger` para info/error — nunca `console.log`.

    Criar `src/lib/jobs/handlers/heartbeat.ts`: handler trivial nomeado `heartbeat` que apenas registra `logger.info("jobs.heartbeat", { at: new Date().toISOString() })`. Serve para validar que o worker está vivo e processa jobs.

    Criar `src/lib/jobs/worker.ts`: entry-point do processo separado. Importa `getBoss`, registra os handlers via `boss.work("heartbeat", handler)`, e mantém o processo vivo. Tratar `SIGTERM`/`SIGINT` chamando `boss.stop()` para graceful shutdown. Este arquivo NÃO é importado pelo bundle do Next.js — roda standalone via `tsx`.

    Em `package.json`: adicionar ao bloco `scripts` a entrada `"jobs:worker": "tsx src/lib/jobs/worker.ts"`, espelhando o estilo de `db:seed`.

    Em `.env.example` (criar se não existir, ou anexar): documentar `JOBS_WORKER_CONCURRENCY=5` e nota de que `pg-boss` usa a mesma `DATABASE_URL`.

  </action>
  <acceptance_criteria>
    - `grep -c "schema.*pgboss" src/lib/jobs/boss.ts` retorna pelo menos 1 (schema isolado configurado)
    - `grep -c "server-only" src/lib/jobs/boss.ts` retorna 1
    - `grep -c "jobs:worker" package.json` retorna 1
    - `grep -c "pg-boss" package.json` retorna pelo menos 1 (dependência instalada)
    - `grep -E "SIGTERM|SIGINT" src/lib/jobs/worker.ts` encontra o tratamento de shutdown
    - `npx tsc --noEmit` termina com exit code 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>pg-boss instalado; singleton com schema isolado; worker standalone com graceful shutdown; script jobs:worker registrado; TSC sem erros.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                            |
| ----------------------------- | -------------------------------------------------------------------------------------- |
| processo Next.js → PostgreSQL | conexão pg-boss compartilha a DATABASE_URL; credenciais não devem vazar                |
| worker process → fila de jobs | jobs enfileirados executam código arbitrário do handler; só handlers registrados rodam |

## STRIDE Threat Register

| Threat ID | Category               | Component              | Disposition | Mitigation Plan                                                                                                          |
| --------- | ---------------------- | ---------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------ |
| T-11-01   | Tampering              | migration v05_fundacao | mitigate    | Migração 100% aditiva (colunas nullable, ADD VALUE) — não altera dados existentes; verificação via prisma migrate status |
| T-11-02   | Denial of Service      | pg-boss schema         | mitigate    | `schema: "pgboss"` isolado evita conflito com modelos Prisma e poluição do schema public                                 |
| T-11-03   | Elevation of Privilege | worker.ts              | mitigate    | Worker só registra handlers nomeados explicitamente; nenhum job dinâmico/eval                                            |

</threat_model>

<verification>
- `npx prisma validate` e `npx prisma migrate status` confirmam schema e migração.
- `npx tsc --noEmit` sem erros.
- `pnpm jobs:worker` inicia, conecta e fica vivo (parar com Ctrl+C aciona graceful shutdown).
- `psql` confirma a existência física das 3 tabelas e das 2 colunas de hash.
</verification>

<success_criteria>

- Migração `v05_fundacao` aplicada ao banco; 3 tabelas novas e colunas prevHash/currentHash existem fisicamente.
- Enum Escopo do banco contém `notificacoes` e `jobs`.
- `pg-boss` instalado; worker roda como processo separado com schema `pgboss` isolado.
- Build TSC verde.
  </success_criteria>

<output>
After completion, create `.planning/phases/11-fundacao-infra-cruzada/11-01-SUMMARY.md`
</output>
